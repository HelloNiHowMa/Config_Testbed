---
- name: 確保 Windows 靶機防火牆永遠處於關閉狀態
  hosts: Windows
  tasks:
    - name: 暴力關閉所有防火牆設定檔
      community.windows.win_firewall:
        state: disabled
        profiles:
          - Domain
          - Private
          - Public
